sudo: required
services:
    - docker

before_install:
    - gem install rake
    - docker build -t mritunjayonemail/docker-react -f Dockerfile.dev .
    
script:
    - docker run mritunjayonemail/docker-react npm run test -- --coverage